# 名称
name: flutter_demo_iterm
# 介绍
description: A new Flutter project.
# 版本号
version: 1.0.0+1

# 可以移除
publish_to: 'none'

# 可以移除
environment:
  sdk: ">=2.7.0 <3.0.0"

# 依赖包
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  # 新添加的依赖包
  english_words: ^3.1.3


# 开发环境的依赖包
dev_dependencies:
  flutter_test:
    sdk: flutter

# Flutter的相关配置
flutter:
  uses-material-design: true
  assets:
    - testPic/my_icon.png
